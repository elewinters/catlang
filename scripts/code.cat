fn meow() {
	syscall!(1, 1, "meow\n", 5);
}

fn print(str: int64, len: int64) {
	syscall!(1, 1, str, len);
}

fn _start() {
	let sys_write: int64 = 1;
	let stdout: int64 = 1;

	syscall!(sys_write, stdout, "Hello, world!\n", 14);
	
	print("hi :D\n", 6);
	meow();

	syscall!(60, 0);
}