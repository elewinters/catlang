fn _start() {
	let sys_write: int64 = 1
	let stdout: int64 = 1;

	let sys_exit: int64 = 60;
	let exit_code: int64 = 0;

	syscall!(sys_write, stdout, "Hello, world!\n", 14);
	syscall!(sys_exit, exit_code);
}