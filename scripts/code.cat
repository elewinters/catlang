extern puts(i64);
extern printf(i64, i32);

extern strcpy(i64, i64);

extern malloc(i64) -> i64;
extern free(i64);

extern exit(i32);
extern system(i32);

fn number() -> i32 {
	puts("gonna return a number :D")'

	return 5 + 5;
}

fn void() -> i32 {
	return 5;
}

fn main() {
	let num: i32 = number();

	asm!("xor rax, rax");
	printf("%d\n", num);

	exit(0);
}
