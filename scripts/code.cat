fn puts(str: i64);
fn printf(fmt: i64, num: i32, c: i8);
fn putchar(c: i8);
fn exit(exit_code: i64);

fn myputchar(c: i8) {
	putchar(c);
}

fn main() {
	let num: i32 = 12/3;

	asm!("xor rax, rax");

	printf("%d: %c\n", num, 67 + 3);

	let A: i8 = 65;
	let n: i8 = 10;

	putchar(A);
	putchar(n);

	myputchar(A);
	myputchar(n);

	putchar(66 + 5);
	putchar(10);

	let string: i64 = "Hello, world!";
	puts(string);

	exit(0);
}
