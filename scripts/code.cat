extern puts(i64);

extern strcpy(i64, i64);

extern malloc(i64);
extern free(i64);

extern exit(i32);
extern system(i32);

fn main() {
	let mem: i64 = malloc(50);
	strcpy(mem, "Hello, world!")

	puts(mem);

	let literal: i32 = "/bin/sh";
	system(literal);

	exit(0);
}
