fn print(str: int64, len: int64) {
	syscall!(1, 1, str, len);
}

fn exit(exit_code: int64) {
	syscall!(60, exit_code);
}

fn _start() {
	print("Hello, world!\n", 14);
	print("yass girl\n", 10);
	print("meow :3\n", 8);
		
	exit(0);
}
