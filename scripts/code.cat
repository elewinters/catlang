fn printf(fmt: i64, n: i32);
fn exit(code: i32);
fn strlen(ptr: i64) -> i32;

fn sum(x: i32, y: i32) -> i32 {
	return x + y;
}

fn main() -> i32 {
	let x: i64 = 5;
	let y: i64 = 10;
	let num: i32 = sum(5, 5);

	asm!("xor rax, rax");
	printf("%d\n", num);

	return 0;
}
