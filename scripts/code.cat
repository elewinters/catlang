fn puts(str: i64);
fn printf(fmt: i64, num: i32);
fn putchar(c: i8);
fn exit(exit_code: i64);

fn atoi(a: i64);

fn main() {
	putchar(65); /* A */
	putchar(10); /* newline */

	let hello: i32 = atoi("10");
	let hello2: i32 = hello;

	asm!("xor rax, rax");
	printf("%d\n", hello2);

	exit(0);
}
